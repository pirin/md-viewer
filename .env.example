# md-viewer configuration
# Copy to .env.local and customize for your project

# --- Content ---
# Comma-separated directories to scan for markdown files (relative to CONTENT_BASE_DIR)
CONTENT_DIRS=reports,plans
# Base directory for content (defaults to one level above viewer/)
# CONTENT_BASE_DIR=/absolute/path/to/content/root

# --- Branding ---
# Brand displayed as: {BRAND_NAME}{BRAND_ACCENT} (accent part is highlighted)
NEXT_PUBLIC_BRAND_NAME=THES
NEXT_PUBLIC_BRAND_ACCENT=AI
NEXT_PUBLIC_TAGLINE=INVESTMENT THESIS & RESEARCH TERMINAL

# --- Site metadata ---
NEXT_PUBLIC_SITE_TITLE=ThesAI Viewer
NEXT_PUBLIC_SITE_DESCRIPTION=AI Crypto Portfolio Manager Research Viewer

# --- Theme ---
# Override the accent color (CSS color value). Defaults to #FF8000 in globals.css.
# NEXT_PUBLIC_ACCENT_COLOR=#FF8000

# --- Storage ---
# Prefix for localStorage keys (e.g. {prefix}-favorites, {prefix}-filter-mode)
NEXT_PUBLIC_STORAGE_PREFIX=thesai

# --- Display ---
# Comma-separated prefixes to strip from filenames in the sidebar/breadcrumbs
NEXT_PUBLIC_FILENAME_STRIP_PREFIXES=Profile-

# Number of days to consider a file "recently updated"
RECENT_DAYS_THRESHOLD=7

# --- Auth ---
# Set a password to require login. Leave empty to disable auth.
# SITE_PASSWORD=
